rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{id} {
      allow read: if true;            // open list (no auth for this task)
      allow create, update: if true;  // client add / edit / status toggle
      allow delete: if false;         // Admin SDK (Cloud Function) only
    }
  }
}
